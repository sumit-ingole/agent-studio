{
  "name": "@agent-studio/types",
  "version": "1.0.0",
  "description": "Shared TypeScript types for Adio",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "clean": "rm -rf dist tsconfig.tsbuildinfo"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
